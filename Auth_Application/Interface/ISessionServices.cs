

namespace IdentityApplication.Interface
{
    public interface ISessionServices
    {
        //Task<SessionStatus> GetUsrerSessionAsync(string userId);
        //List<SessionStatus> GetUsrerAllSessions(string userId);
        //Task Removeasync(SessionStatus entity);
        //Task AddSessionAsync(SessionStatus session);
    }
}
